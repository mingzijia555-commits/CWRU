# CWRU 轴承故障诊断完整实施计划

## 最终交付

所有内容在 `D:\Projects\人工智能实践课\CWRU` 中完成，最终交付：

- 模块化 PyTorch 源代码
- 109 个原始 MAT 数据文件
- 10 个最佳推理权重
- 10 个最后续训检查点
- 固定数据划分、归一化参数和实验配置
- 训练历史、测试指标、预测结果
- 混淆矩阵及模型对比图片
- 环境配置、运行说明和自动测试
- 私有仓库 `mingzijia555-commits/CWRU`

不开发图形交互界面。训练、评估和推理通过终端执行；结果图自动生成，供报告使用。

## GitHub 仓库

- 在现有 `CWRU` 文件夹初始化 Git。
- 通过已登录的 `mingzijia555-commits` 账号创建同名私有仓库。
- 使用普通 Git 提交原始数据，不使用 Git LFS。
- 上传代码、数据、最终权重、续训检查点、指标和图表。
- 忽略缓存、临时窗口文件、Python 缓存和训练过程中的非最终检查点。
- MAT 原始文件首次提交后视为只读，避免二进制历史膨胀。
- 自动测试全部通过后，即创建私有仓库并推送首个版本（代码、原始数据、清单和测试），MAT 文件此后视为只读。
- 完成最终训练和评估后，提交发布版本并再次推送 `main`。

## 数据预处理

- 数据路径固定为项目内的 `CaseWesternReserveUniversityData/`。
- 特殊变量映射：
  - `normal_2_99.mat` 选择 `X099_DE_time/X099_FE_time`。
  - 修复后的 `12k_Fan_End_IR014_1_276.mat` 选择 `X275_*`。
  - 28 mil 文件从实际内容中识别唯一 `*_DE_time`。
- Normal 原始信号按 48 kHz 处理，使用 `scipy.signal.resample_poly(signal, 1, 4)` 降到 12 kHz。
- 故障数据保持原始 12 kHz。
- 统一窗口长度和步长为 1024，不重叠，尾部不足部分丢弃。
- 归一化参数仅从各实验训练集计算。
- 预期窗口数：
  - 101 文件方案：约 11,891
  - 109 文件方案：约 12,833

## 文件划分

- 必须先划分文件，再切分窗口。
- 101 文件公共集合固定为 `71/15/15`。
- Normal、B028、IR028 分别采用 `2/1/1`。
- 在公共划分上加入 B028 和 IR028 后，109 文件集合为 `75/17/17`。
- 公共文件在 101 和 109 清单中的归属完全一致。
- 固定随机种子 42，并尽量平衡故障端、负载、直径和外圈位置。
- 划分生成后保存为固定清单，所有模型禁止重新随机划分。

## 实验配置

按以下顺序执行，每种配置先训练 `Cnn1d`，再训练 `CnnGru`：

1. `双端`：101 文件，输入 `[DE, FE]`
2. `109DEFE`：驱动端故障取 DE，风扇端故障取 FE，正常取 DE
3. `101DEFE`：排除 28 mil，其余同上
4. `109DE`：109 文件全部取 DE
5. `101DE`：101 文件全部取 DE

共进行 10 次正式训练。`DEFE` 是已知测量端条件下的单通道方案，不负责自动判断故障端。

28 mil 保持原方案，正常参与分类和回归训练；不加入厂家或故障深度输入。代码单独输出 28 mil 和文件级指标，明确其在当前 CWRU 数据分布下的表现。

## 模型设计

### Cnn1d

- 三个 Conv1d 模块，通道为 `32/64/128`
- 卷积核为 `7/5/3`
- BatchNorm、ReLU、MaxPool
- 自适应平均池化得到共享特征

### CnnGru

- 使用相同 CNN 提取局部特征
- 单层双向 GRU，单方向隐藏维度 64
- 拼接双向状态作为共享特征

两种模型均输出：

- `Normal/IR/OR/B` 四分类 logits
- 一个连续故障直径值

回归标签使用 `diameter_mil / 7` 缩放，输出时乘以 7。

## 训练规则

- 分类使用逆平方根类别权重：

  `weight[c] = 1 / sqrt(training_window_count[c])`

- 权重归一化到平均值 1。
- 保留全部窗口，不进行均衡抽样或 OR 随机删减。
- 所有样本参与分类损失；Normal 不参与回归损失。
- 回归使用 Smooth L1，总损失为分类损失加回归损失。
- AdamW，学习率 `1e-3`，权重衰减 `1e-4`。
- Batch Size 128，最多 80 个 epoch。
- ReduceLROnPlateau 耐心值 4，Early Stopping 耐心值 12，学习率衰减因子 0.5。
- 早停、ReduceLROnPlateau 和 `best_inference.pt` 的保存统一以验证集总损失（分类损失 + 回归损失）为监控指标。
- 固定种子 42，不使用数据增强。
- 每次训练保存：
  - `best_inference.pt`
  - `last_training.ckpt`
  - 训练历史和完整配置

## 终端接口

提供统一命令入口：

- `audit-data`：检查文件、采样率、变量和特殊映射
- `prepare`：生成元数据、划分和归一化参数
- `train`：训练指定实验和模型
- `run-all --resume`：依次完成或恢复全部 10 次训练
- `evaluate`：生成完整测试指标和图表
- `compare`：汇总比较 10 组实验
- `predict`：加载权重并对指定 MAT 文件推理

推理命令在终端打印：

- 文件名和所用通道
- 预测故障类别
- 四个类别的概率
- 回归头原始直径
- 最终显示直径
- 文件内各窗口统计摘要

如果预测类别为 Normal，最终直径显示为 0。`DEFE` 模型推理时必须显式指定 DE 或 FE。

## 自动生成的图表

- 分类损失、回归损失和总损失曲线
- 验证 Accuracy、Macro-F1、MAE 和 RMSE 曲线
- 每个模型的混淆矩阵
- 各类别 Precision、Recall、F1
- 各直径档位 MAE/RMSE
- OR 在 `3/6/12` 点钟位置的细分表现
- 10 组实验的分类指标对比图
- 10 组实验的回归指标对比图
- 窗口级与文件级指标对比

同时保存对应 CSV 和 JSON，保证报告中的图表可以重新生成。

## 测试与验收

自动测试覆盖：

- 109/101 文件识别和特殊变量映射
- Normal 48→12 kHz 抗混叠降采样
- 文件划分无交集且公共划分一致
- 窗口数量、长度、步长和通道形状
- 训练集独占的归一化和类别权重计算
- Normal 回归掩码
- 两种模型前向、反向、保存和加载
- 最佳权重推理
- 最后检查点断点续训
- 小规模端到端训练、评估和绘图

最终验收条件：

- `run-all --resume` 可以完成或恢复全部 10 次训练。
- 20 个模型文件均能正常加载。
- 任意测试 MAT 文件都能通过终端完成推理。
- 所有指标、CSV 和报告用图片均能自动重新生成。
- 私有 GitHub 仓库包含最终可复现版本。

## 已确认的执行环境（2026-09-15 勘察结果）

- 使用 conda 环境 `AI2026Summer`：`D:\Softwares\Miniconda\envs\AI2026Summer\python.exe`（Python 3.11.15）。系统默认 `python` 是 3.14 且未装 torch，禁止使用。
- torch 2.11.0+cu128，CUDA 可用（RTX 5070 Laptop 8GB）；scipy 1.16.0、numpy 2.3.1、matplotlib 3.11.0、pandas 3.0.5 均已就绪。
- `gh` CLI 已登录 `mingzijia555-commits`，token 含 `repo` 权限，可直接创建私有仓库。
- 109 个 MAT 文件的完整性和正确性已由用户人工确认，特殊变量映射抽查与计划一致（`normal_2_99.mat` 内含 X098/X099 两组变量、`12k_Fan_End_IR014_1_276.mat` 内含 X275、8 个 28 mil 文件仅有随机命名的 DE 通道）；执行时无需再做全量数据审计。

## 已确认的约束与说明

- 28 mil 数据继续保留。其轴承厂家和故障深度与前三档不同，但本项目以当前 CWRU 数据分布下的预测效果为目标。
- Normal 数据统一降采样到 12 kHz，以消除可修正的采样率差异。
- 外圈数据全部保留，通过逆平方根类别权重降低类别数量不平衡的影响。
- 不增加厂家、故障深度、负载、RPM、BA 或外圈位置作为模型输入；这些字段仅用于划分和结果分析。
- 不开发 UI；演示采用终端推理，报告使用自动生成的图片和指标。
